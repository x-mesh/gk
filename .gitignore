bin/
dist/
*.test
*.out
coverage.out
.DS_Store
.env
.idea/
.vscode/

# AI / project harness state (not part of the product)
# .claude/skills/ is tracked so /release etc. are shared; everything else is local.
.claude/*
!.claude/skills/
.omc/
.xm/

# Compiled binaries at root (for local dev)
/gk
.kiro/
.cache/
.xm/

# Security
.env.*
*.pem
id_rsa*
credentials.json
*.pfx
*.kdbx
*.keystore
service-account*.json

# IDE/Editor
.cursor/
# NOTE: .claude is handled above (.claude/* with a !.claude/skills/ exception).
# Do NOT re-add a bare `.claude/` here — excluding the whole dir defeats the
# skills negation and silently un-tracks shared skills like /release, /roadmap.
*.swp
Thumbs.db

# Compiled artifacts
__pycache__/
*.pyc
*.pyo
*.pyd
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.class
*.o
*.obj
*.so
*.dylib
*.dll
*.a
*.lib
*.cover
*.coverage
coverage/
.tox/

# Local agent/tool state (not project content)
.antigravitycli/
