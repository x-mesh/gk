---
inclusion: always
---
# Repository Structure

## Top-level layout
```
.
├── cmd/            # Executable entry points
├── internal/       # Private application packages
├── pkg/            # Public reusable packages
├── docs/           # Documentation
├── testdata/       # Fixtures and golden files
└── <!-- TODO: add/remove directories to match your repo -->
```

## Key files
<!-- TODO: List files the AI must know about to navigate this codebase effectively -->
| File / Dir | Role |
|-----------|------|
| `<!-- path -->` | <!-- what it does --> |

## Where to put new code
<!-- TODO: Decision rules for where new logic belongs -->
- New CLI command → `<!-- e.g. internal/cli/<command>.go -->`
- New business logic → `<!-- e.g. internal/<package>/ -->`
- New shared utility → `<!-- e.g. pkg/<package>/ -->`
- Tests → alongside the file they test, `_test.go` suffix

## Generated files
<!-- TODO: List any files that are auto-generated and must NOT be edited by hand -->
- `<!-- file -->` — generated by `<!-- tool -->`; edit `<!-- source -->` instead

## Naming conventions
<!-- TODO: File, package, and symbol naming rules -->
- Files: `snake_case.go`
- Packages: short, lowercase, no underscores
- Exported types: `PascalCase`; unexported: `camelCase`

## Module / package boundaries
<!-- TODO: Describe import rules, forbidden dependencies, layering constraints -->
- `internal/cli` may import `internal/*` and `pkg/*`
- `pkg/*` must NOT import `internal/*`
- <!-- TODO: other rules -->
